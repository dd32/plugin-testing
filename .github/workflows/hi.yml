# This is a basic workflow to help you get started with Actions

name: Hey DD32 Your Rock EH!

  push:
    branches: [ master ]
  pull_request:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Total pwnage"
